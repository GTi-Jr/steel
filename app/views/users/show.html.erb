<h3><%= @user.username %></h3>
<h4><%= @user.email %></h4>
<p><%= link_to "Criar projeto para #{@user.username}", new_project_path %></p>

<% @user.projects.each do |project| %>
  <p><%= link_to project.name, project_path(project) %></p>
<% end %>

<%= link_to "Voltar", root_path %>
<%= link_to "Início", root_path %>
